<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="res_partner_inherited_form" model="ir.ui.view">
        <field name="name">res.partner.inherited.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Website" name="website_page">
                    <group>
<!--                        <field name="is_published"/>-->
                        <field name="description" /><!-- attrs="{'required': [('is_published','=', True)]}"-->
                        <field name="website_image" widget="image" class="oe_avatar"/>
                        <field name="web_image" widget="image" class="oe_avatar"/>
                    </group>
                </page>
            </notebook>
        </field>
    </record>
</odoo>